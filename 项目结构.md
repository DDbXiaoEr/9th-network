# 项目结构

> 第九网络组（The 9th Network Team）社团官网 —— 基于 Vue 3 + Vite 的单页应用（SPA）
> 首页由多个 section 组件纵向组合，`/directions` 为独立的「兴趣方向 / CTF 战队」页。

## 目录树及文件用途

```
.
├── public/                                 # 静态资源目录，构建时原样拷贝
│   ├── config/                             # 运行时内容配置（部署后可直接修改，无需重新构建）
│   │   ├── site.json                       # 站点通用数据：品牌、导航、页脚、首屏、社团、资源、加入
│   │   ├── services.json                   # 社团公共服务数据与分类
│   │   ├── activities.json                 # 活动通知 / 历史数据与 API 地址
│   │   └── directions.json                 # 兴趣方向与 CTF 战队数据
│   └── images/                             # 站点图片素材
│       ├── t9.gif                          # 社团 Logo（导航栏、页脚使用）
│       ├── 1.jpg / 2.jpg / 3.jpg           # 首页首屏轮播背景图（data/site.js -> heroSlides）
│       ├── 1.png / 2.png / 3.png / 4.png   # 资源板块分类图标（data/site.js -> resourceGroups）
│       ├── t91.jpg                         # 社团简介配图 / Windows 安装教程封面
│       ├── t92.jpg                         # 社团发展配图 / 不安全 WiFi 文章封面
│       ├── t93.jpg                         # 社会工程学文章封面
│       ├── t94.jpg / t95.jpg               # 社团成员配图
│       ├── erweima.png                     # 二维码（未引用）
│       ├── hh.jpg                          # 遗留素材（未引用）
│       ├── huidaodingbu1.png               # 返回顶部图标素材（未引用）
│       ├── huidaodingbu2.png               # 返回顶部图标素材（未引用）
│       └── weixin.png                      # 微信图标（未引用）
├── src/                                    # 源码目录
│   ├── components/                         # 可复用 UI 组件
│   │   ├── NavBar.vue                      # 顶部导航栏（移动端菜单、锚点滚动、路由跳转）
│   │   ├── HeroSection.vue                 # 首页首屏：背景轮播 + 标题 + 网络动画
│   │   ├── NetworkCanvas.vue               # Canvas 粒子网络连线动画（首屏背景）
│   │   ├── AboutSection.vue                # 社团介绍：简介 / 成员 / 发展 选项卡
│   │   ├── ActivityTimeline.vue            # 活动通知与往期回顾时间线
│   │   ├── ServicesSection.vue             # 社团公共服务：分类筛选、卡片、详情弹窗
│   │   ├── ResourcesSection.vue            # 资源分享：分组列表 + 文章阅读弹窗
│   │   ├── JoinSection.vue                 # 加入我们：招新说明与报名流程
│   │   ├── SiteFooter.vue                  # 页脚：导航链接、版权信息
│   │   ├── BackToTop.vue                   # 返回顶部悬浮按钮
│   │   └── BaseIcon.vue                    # 内置 SVG 图标库组件（按 name 渲染）
│   ├── composables/
│   │   └── useActivities.js                # 活动数据加载逻辑：优先 API，失败回退本地配置
│   ├── config/                             # 内容配置
│   │   ├── index.js                        # 运行时配置加载器：优先请求 /config/*.json，失败回退本地默认值
│   │   ├── activities.js                   # 活动通知默认数据（兜底）
│   │   ├── directions.js                   # 兴趣方向与 CTF 战队默认数据（兜底）
│   │   └── services.js                     # 社团公共服务默认数据（兜底）
│   ├── data/
│   │   └── site.js                         # 站点通用数据默认值（兜底）：品牌、导航、页脚、轮播、统计、社团、资源、加入
│   ├── router/
│   │   └── index.js                        # 路由配置（hash 模式：首页 / 兴趣方向）
│   ├── views/                              # 页面级组件（路由目标）
│   │   ├── HomeView.vue                    # 首页：按顺序组合各 section 组件
│   │   └── DirectionsView.vue              # 兴趣方向页：方向介绍 + CTF 战队板块
│   ├── App.vue                             # 根组件：导航 + <router-view> + 页脚 + 返回顶部
│   ├── main.js                             # 应用入口：创建实例、注册路由与 v-reveal 指令
│   └── style.css                           # 全局样式与设计变量（颜色、间距、动画）
├── index.html                              # HTML 入口模板（SEO 元信息、挂载点）
├── package.json                            # 项目依赖与 npm 脚本（dev / build / preview）
├── package-lock.json                       # 依赖版本锁定文件
├── vite.config.js                          # Vite 构建配置（Vue 插件、端口 5173）
├── .gitignore                              # Git 忽略规则
└── 项目结构.md                             # 本说明文档
```

## 内容配置

站点的全部可配置内容都放在 `public/config/` 下的 JSON 文件中，应用启动时会从站点根目录
（即 `/config/site.json`、`/config/services.json`、`/config/activities.json`、
`/config/directions.json`）运行时加载。

- 修改内容：编辑 `public/config/*.json`，开发模式下保存即热更新，发布前执行 `npm run build`。
- 部署后更新：直接修改服务器上 `dist/config/*.json` 并刷新页面即可，无需重新构建。
- 兜底策略：任一 JSON 加载失败或字段缺省时，自动回退到打包进应用的本地默认值
  （`src/data/site.js` 与 `src/config/*.js`），页面不会白屏。

## 常用命令

```bash
npm install     # 安装依赖
npm run dev     # 本地开发（默认 http://localhost:5173）
npm run build   # 构建生产版本到 dist/
npm run preview # 本地预览构建结果
```
